#include <bits/stdc++.h>
using namespace std;

//? ฟังก์ชันแบ่งส่วน (Partition) → จัด pivot ให้อยู่ตำแหน่งที่ถูกต้อง, และคืน index ของ pivot กลับมา
int partition(int arr[], int low, int high){
    int pivot = arr[low]; //* ใช้ตัวแรกของช่วงเป็น pivot
    //@ i → วิ่งจากซ้ายไปขวา, j → วิ่งจากขวาไปซ้าย
    int i = low; 
    int j = high + 1; //* ให้เริ่มที่ high + 1 เพื่อให้ลูปข้างล่างทำงานถูกต้อง(decrement ก่อนใช้งาน)
    while(true){ //* ลูปนี้จะทำงานจนกว่า i และ j จะชนกัน
        //@ ขยับ i → เลื่อน i ไปทางขวา
        do{ 
            i++;
        } while(i <= high && arr[i] < pivot); 
        /* หยุดเมื่อ: 
            ● i ชนขอบขวา → เกิน high
              ! กรณีที่ไม่ดักไว้ อาจทำให้ array out of bound แต่อาจจะไม่ Crash เพราะอาจจะไปอ่านค่าขยะที่มากกว่า pivot ก็ได้
            ● เจอค่าที่ มากกว่าหรือเท่ากับ pivot */ 
        //@ ขยับ j → เลื่อน j ไปทางซ้าย
        do{ 
            j--;
         } while(arr[j] > pivot); //* หยุดเมื่อเจอค่าที่ น้อยกว่าหรือเท่ากับ pivot
        if(i >= j) break; //* ถ้า i กับ j สวนทางกันแล้ว ให้หยุด → แบ่งเสร็จแล้ว
        swap(arr[i], arr[j]); //* ค่าทางซ้ายที่ใหญ่เกิน → ไปอยู่ขวา, ค่าทางขวาที่เล็กเกิน → มาอยู่ซ้าย
    }
    //* สลับ pivot (ตำแหน่ง low) ไปไว้ที่ตำแหน่ง j (ตำแหน่งที่ถูกต้อง)
    swap(arr[low], arr[j]);
    return j; //* คืนค่า pivot index → j คือจุดที่ pivot ควรอยู่
}

void quickSort(int arr[], int low, int high){
    if(low < high){ 
        int pi = partition(arr, low, high);
        quickSort(arr, low, pi - 1);
        quickSort(arr, pi + 1, high);
    }
}

int main(){
    int arr[] = {8, 3, 1, 7, 0, 10, 2};
    int n = sizeof(arr) / sizeof(arr[0]);
    quickSort(arr, 0, n - 1);
    for(int i = 0; i < n; i++){
        cout<<arr[i]<<" ";
    }
    cout<<endl;
    return 0;
}

/*
- Quick Sort คืออัลกอริทึมการเรียงลำดับที่เร็วและนิยมใช้มากที่สุดตัวหนึ่ง โดยใช้หลักการ "Divide and Conquer"
  (แบ่งแยกและเอาชนะ)
@ แนวคิดหลักของ Quick Sort (คิดแบบคลาสสิก)
- Quick Sort ใช้แนวคิด Divide and Conquer (แบ่งแล้วพิชิต)
* 1. เลือกตัวหนึ่งเป็น Pivot
* 2. จัดเรียงให้
     ● ค่าที่ น้อยกว่า Pivot ไปอยู่ซ้าย
     ● ค่าที่ มากกว่า Pivot ไปอยู่ขวา
* 3. ทำแบบเดิมซ้ำ(Recursive) กับฝั่งซ้าย และฝั่งขวาหยุดเมื่อเหลือข้อมูล 0 หรือ 1 ตัว (เรียงอยู่แล้ว)
- จุดเด่นคือ ไม่ต้องใช้หน่วยความจำเพิ่มเยอะ และโดยเฉลี่ยเร็วมาก
@ ประสิทธิภาพ (Time Complexity)
* 1. กรณีดีที่สุด (Best Case): O(nlog n) – เกิดขึ้นเมื่อ Pivot สามารถ "แบ่งข้อมูลออกเป็น 2 ฝั่งได้เท่าๆ กัน"
    (หรือใกล้เคียงกัน) เสมอ
    ● T(n) = 2T(n/2) + n
        - 2T(n/2) คือเราต้องแก้ปัญหาย่อย 2 ฝั่ง ซึ่งแต่ละฝั่งมีขนาดเหลือครึ่งเดียว 
        - n คือเวลาที่ใช้ในการ Partition (วนลูปเทียบข้อมูลทุกตัวกับ Pivot ในรอบนั้น)
* 2. กรณีทั่วไป (Average Case): O(nlog n) – เร็วมาก และใช้บ่อยที่สุด
* 3. กรณีแย่ที่สุด (Worst Case): O(n^2) – เกิดขึ้นเมื่อข้อมูลเรียงลำดับมาอยู่แล้ว แล้วเราเลือกตัวสุดท้ายเป็น 
     Pivot เสมอ (แต่แก้ไขได้โดยการสุ่มเลือก Pivot)
    ● T(n) = T(n - 1) + n
        - T(n - 1) ปัญหาลดขนาดลงไปแค่ 1 ตัว (เหมือนปอกเปลือกหัวหอมทีละชั้น)
        - n เวลาที่ใช้ Partition
@ ตัวอย่างการทำงานตามโค้ด (Pivot ที่ตำแหน่งแรก)
- สมมติมีตัวเลข: [10, 80, 30, 90, 40] เราเลือกตัวแรก 10 เป็น Pivot
    * รอบที่ 1: pivot = 10, low = 0, high = 4 → [10, 80, 30, 90, 40]
        ● i เริ่มที่ 0, j เริ่มที่ 5
        ❌ ขยับ i → เจอ 80 (หยุด) → หยุดที่ i = 1
        ✅ ขยับ j → เจอ 40, 90, 30, 80 → j = 1
        ❌ ขยับ j → เจอ 10 (หยุด) → หยุดที่ j = 0
        ● i กับ j สวนทางกัน → หยุด
        ● สลับ pivot (10) กับ arr[j(0)] (10) → ได้ [10, 80, 30, 90, 40]
        ● แบ่งเป็น 2 ฝั่ง: [ ] กับ [80, 30, 90, 40]
    * รอบที่ 2: pivot = 80, low = 1, high = 4 → [80, 30, 90, 40]
        ● i เริ่มที่ 1, j เริ่มที่ 5
        ✅ ขยับ i → เจอ 30 → i = 2
        ❌ ขยับ i → เจอ 90 (หยุด) → i = 3
        ❌ ขยับ j → เจอ 40 (หยุด) → j = 4
        ● สลับ arr[i] (90) กับ arr[j] (40) → ได้ [10, 80, 30, 40, 90]
        ❌ ขยับ i → เจอ 90 (หยุด) → i = 4
        ❌ ขยับ j → เจอ 40 (หยุด) → j = 3
        ● i กับ j สวนทางกัน → หยุด
        ● สลับ pivot (80) กับ arr[j(3)] (40) → ได้ [10, 40, 30, 80, 90] 
        ● แบ่งเป็น 2 ฝั่ง: [40, 30] กับ [90]
    * รอบที่ 3: pivot = 40, low = 1, high = 2 → [40, 30]
        ● i เริ่มที่ 1, j เริ่มที่ 3
        ✅ ขยับ i → เจอ 30 → i = 2
        ❌ ขยับ i → i = 3 (เกิน high)
        ❌ ขยับ j → เจอ 30 (หยุด) → j = 2
        ● i กับ j สวนทางกัน → หยุด
        ● สลับ pivot (40) กับ arr[j(2)] (30) → ได้ [10, 30, 40, 80, 90]
        ● แบ่งเป็น 2 ฝั่ง: [30] กับ [ ]
- สุดท้ายได้อาร์เรย์ที่เรียงลำดับแล้ว: [10, 30, 40, 80, 90]
*/