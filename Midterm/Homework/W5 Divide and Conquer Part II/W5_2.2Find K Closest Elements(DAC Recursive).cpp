#include <bits/stdc++.h>
using namespace std;

//? ฟังก์ชันหาตำแหน่งเริ่มต้นของ k (ตัวที่ค่าใกล้เคียงกับ target)
int findClosestElements(vector<int>& arr, int k, int target){
    int n = arr.size();
    int low = 0;
    int high = n - k; //! จุดเริ่มต้นที่เป็นไปได้สูงสุด (เพราะถ้าจุดเริ่มเกินนี้ จะได้สมาชิกไม่ครบ k ตัว)
    //@ Divide and Conquer: Binary Search หาจุดเริ่มต้นที่ดีที่สุด
    //! เป้าหมาย: เราต้องการบีบช่วง [low, high] ให้เล็กลงเรื่อยๆ จนกระทั่ง low กับ high มาบรรจบกันที่จุดเดียว (Converge) จุดนั้นคือคำตอบ
    while(low < high){
        int mid = (low + high) / 2;
        //* หน้าต่าง [i ... i+k-1] ไหน “ใกล้ target มากที่สุด”
        //* ตัวซ้ายสุดของ window ปัจจุบัน → arr[mid]
        //* ตัวขวาสุดของ window ถัดไป → arr[mid+k]
        //* ตัวไหนใกล้กว่า → ขยับ window ไปทางนั้น
        //* เช็คว่าหน้าต่างทางขวา (เริ่มที่ mid+1) ดีกว่าหน้าต่างปัจจุบัน (เริ่มที่ mid) หรือไม่
        if(abs(arr[mid + k] - target) < abs(arr[mid] - target))
            //* ถ้าตัวถัดไป (mid+k) ค่าใกล้ target มากว่ากว่าตัวปัจจุบัน (mid) -> คำตอบต้องอยู่ทางขวา
            low = mid + 1; //! ตัด mid ทิ้งได้ เพราะมันแย่กว่า mid+k
        else
            //* ถ้าตัวถัดไป (mid) ค่าใกล้ target มากว่าหรือเท่ากับตัวปัจจุบัน (mid + k) -> คำตอบต้องอยู่ทางซ้าย
            high = mid; //! ตัด mid ทิ้งไม่ได้ เพราะ arr[mid] <= arr[mid+k] ตำแหน่ง mid อาจจะดี
    }
    return low; //* คืนค่า index เริ่มต้นที่ดีที่สุด
}

int main(){
    vector<int> arr = {10, 12, 15, 17, 18, 20, 25};
    int k = 2;
    int target = 8;
    //@ เรียงข้อมูล 
    sort(arr.begin(), arr.end());
    //* หาจุดเริ่มต้นด้วย Binary Search (O(log n))
    int startIndex = findClosestElements(arr, k, target);
    //* แสดงผลลัพธ์ (O(k))
    cout<<"Result: ";
    for (int i = startIndex; i < startIndex + k; i++)
        cout<<arr[i]<<" ";
    cout<<endl;
    return 0;
}

/*
@ หลักการ
* 1. ทำการเรียงลำดับข้อมูลก่อน
* 2. กำหนดขอบเขตการค้นหา (Search Space) ของ "จุดเริ่มต้น" ที่เป็นไปได้
     ● low = 0
     ● high = n - k (จุดเริ่มต้นสูงสุดที่เป็นไปได้ เพราะถ้าเกินนี้สมาชิกจะไม่ครบ k ตัว)
* 3. เริ่มกระบวนการ Binary Search เพื่อหาจุดเริ่มต้นที่ดีที่สุด (Window Optimization)
    ● เป้าหมายคือบีบช่วง [low, high] จนเหลือจุดเดียว (Converge)
    ● คำนวณ mid = (low + high) / 2
* 4. เปรียบเทียบเพื่อตัดสินใจเลือกช่วงคำตอบ (Slide Window Decision)
- พิจารณาว่าควรขยับหน้าต่างไปทางขวา (เริ่มที่ mid+1) หรืออยู่ที่เดิม (เริ่มที่ mid)
- ใช้เงื่อนไข: abs(arr[mid + k] - target) < abs(arr[mid] - target)
- คือการเทียบว่า "ตัวที่จะเข้ามาใหม่ทางขวา (mid+k)" ใกล้เป้าหมายมากกว่า "ตัวที่จะหลุดออกทางซ้าย (mid)" หรือไม่?
    ● 4.1 กรณีตัวขวาใกล้กว่า (Right is better) → แสดงว่าคำตอบที่ดีกว่าอยู่ทางขวา → ขยับ low ขึ้น 
          (low = mid + 1)
    ● 4.2 กรณีตัวซ้ายใกล้กว่าหรือเท่ากัน (Left is better/equal) → แสดงว่าคำตอบที่ดีกว่าอยู่ทางซ้าย 
          (หรือคือตัวปัจจุบัน) → ขยับ high ลง (high = mid)
* 5. จบลูปเมื่อ low == high
    ● คืนค่า low เป็น index เริ่มต้นที่ดีที่สุด
* 6. แสดงผลลัพธ์ → วนลูปพิมพ์ข้อมูล k ตัว เริ่มจาก index ที่ค้นหาได้ (startIndex ถึง startIndex + k - 1)
@ Time Complexity
- T(n) = O(log(n - k)) + O(k) //* หมายเหตุ: O(k) เกิดขึ้นเฉพาะตอนแสดงผล (Print) ไม่ใช่ตอนค้นหา
- G(n) = O(log n)
- อธิบาย: อัลกอริทึมลดขอบเขตการค้นหาลงทีละครึ่ง (Binary Search) โดยไม่ขึ้นกับค่า k ในขั้นตอนการค้นหา 
         ทำให้ทำงานได้เร็วกว่าแบบเดิมเมื่อ k มีขนาดใหญ่
@ ทำไมขยับแบบ low = mid + 1 และ high = mid ?
- สูตรเช็คคือ: arr[mid+k] (ตัวนอกหน้าต่างทางขวา) ใกล้กว่า arr[mid] (ตัวซ้ายสุดของหน้าต่าง) หรือไม่?
* กรณีที่ 1: low = mid + 1 (ขยับหนีขวา)
    ● เงื่อนไข: abs(arr[mid+k] - x) < abs(arr[mid] - x)
    ● ความหมาย: ตัวเลขทางขวา (mid+k) อยู่ใกล้เป้าหมายมากกว่าตัวทางซ้าย (mid)
    ● การตัดสินใจ: แสดงว่าช่วงคำตอบที่ดีที่สุด "ต้องไม่ใช่" ที่ mid แน่นอน (เพราะแค่ขยับไป mid+1 ก็ดีกว่า 
                 mid แล้ว)
    ● ผลลัพธ์: เราจึงกล้าตัด mid ทิ้ง แล้วไปเริ่มค้นหาที่ mid + 1 ได้เลย
* กรณีที่ 2: high = mid (ดึงซ้ายกลับมา)
    ● เงื่อนไข: abs(A[mid+k] - x) >= abs(A[mid] - x)
    ● ความหมาย: ตัวเลขทางซ้าย (mid) ดีกว่าหรือพอๆ กับทางขวา แสดงว่าคำตอบอาจจะอยู่ทางซ้าย หรือ 
                "อาจจะเป็นที่ mid เองก็ได้"
    ● การตัดสินใจ:
        - เราไม่สามารถใช้ high = mid - 1 ได้ เพราะถ้าคำตอบที่ถูกต้องคือ mid พอดี เราจะเผลอตัดคำตอบที่
          ถูกทิ้งไป
        - เราจึงทำได้แค่บีบขอบขวาลงมาอยู่ที่ mid เพื่อบอกว่า "อย่างน้อยที่สุด คำตอบไม่เกินตำแหน่งนี้นะ แต่
           ตำแหน่งนี้ก็ยังมีลุ้นเป็นคำตอบอยู่นะ"
* สรุปภาพรวม (Visual)
- สมมติเรามี index ที่เป็นไปได้ 0 1 2 3 4 และ mid คือ 2
1. ถ้า mid แย่กว่าตัวขวา: แปลว่าคำตอบต้องอยู่ทางขวาของ 2 แน่ๆ (3, 4)
   → low = mid + 1
2. ถ้า mid ดีกว่าตัวขวา: แปลว่าคำตอบอาจจะเป็น 2 เอง หรือ 0, 1 ก็ได้
   → high = mid (เก็บ 2 ไว้พิจารณาต่อ ห้ามตัดทิ้ง)
3. ทำวนไปจน low กับ high ชนกัน นั่นคือคำตอบครับ
*/

