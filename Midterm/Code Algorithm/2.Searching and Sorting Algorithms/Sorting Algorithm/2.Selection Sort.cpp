#include <bits/stdc++.h>
using namespace std;

void selectionSort(vector<int>& arr){
    int n = arr.size();
    //@ วนลูปเพื่อระบุตำแหน่งที่เราจะเอาตัวน้อยที่สุดมาวาง
    for(int i = 0; i < n - 1; i++){ //* ชี้ตำแหน่งที่จะ “ล็อกค่าให้ถูก”
        //* สมมติให้ตำแหน่ง i เป็นตัวที่น้อยที่สุดไว้ก่อน
        int minIndex = i;
        //* วนลูปหาตัวที่น้อยที่สุดจริงๆ ในส่วนที่เหลือ (i+1 ถึง n)
        for(int j = i + 1; j < n; j++){ //* ใช้หา ค่าที่เล็กที่สุด ในส่วนที่ยังไม่ได้เรียงข้อมูล
            if(arr[j] < arr[minIndex]){
                minIndex = j; //* เจอตัวน้อยกว่า! จดตำแหน่งเก็บไว้
            }
        }
        //* เมื่อหาจนทั่วแล้ว ให้สลับตัวที่น้อยที่สุด (arr[minIndex]) มาไว้ที่ตำแหน่ง i
        if(minIndex != i) //! (แต่ถ้า minIndex ยังเป็น i เหมือนเดิม ก็ไม่ต้องสลับให้เสียเวลา)
            swap(arr[minIndex], arr[i]);
        //* จบ 1 รอบ ได้ตัวเลขที่ถูกต้องมาวางที่ตำแหน่ง i เรียบร้อย
    }
}

int main(){
    vector<int> data = {64, 25, 12, 22, 11};
    cout<<"Original: ";
    for(int x : data) 
        cout<<x<<" ";
    cout<<endl;
    selectionSort(data);
    cout<<"Sorted:   ";
    for(int x : data) 
        cout<<x<<" ";
    cout<<endl;
    return 0;
}

/*
- Selection Sort คืออัลกอริทึมเรียงลำดับที่มีแนวคิดง่ายมาก ตือ เลือกค่าที่เล็กที่สุดจากส่วนที่ยังไม่เรียง
  แล้วเอามาไว้ตำแหน่งด้านหน้า ชื่อมันตรงตัวเลย — Selection = เลือก
แล้วเอามาไว้ตำแหน่งด้านหน้า
@ แนวคิดการทำงาน (หัวใจของ Selection Sort)
* 1. แบ่ง array ออกเป็น 2 ส่วน
    ● ซ้าย: เรียงแล้ว
    ● ขวา: ยังไม่เรียง
* 2. ทุก ๆ รอบ:
    ● หา ค่าน้อยที่สุด ในส่วนที่ยังไม่เรียง
    ● สลับกับตำแหน่งแรกของส่วนนั้น (กรณีที่ค่าที่น้อยที่สุดไม่ใช่ตำแหน่งแรก)
@ Time Complexity (ความซับซ้อนของเวลา)
- O(n^2) ในทุกกรณี (Best, Average, Worst) เพราะต่อให้ข้อมูลเรียงมาเสร็จแล้ว (Best Case) มันก็ยังซื่อบื้อ
  "วนลูปหาตัวน้อยที่สุด" ในส่วนที่เหลืออยู่ดี มันไม่รู้ตัวว่าเรียงเสร็จแล้ว
@ ตัวอย่าง
arr = [64, 25, 12, 22, 11]
    ● รอบที่ 1 (i = 0), min = 11 → [11, 25, 12, 22, 64]
    ● รอบที่ 2 (i = 1), min = 12 → [11, 12, 25, 22, 64]
    ● รอบที่ 3 (i = 2), min = 22 → [11, 12, 22, 25, 64]
    ● รอบที่ 4 (i = 3), min = 25 → [11, 12, 22, 25, 64]
- ผลลัพธ์: arr = [11, 12, 22, 25, 64]
*/