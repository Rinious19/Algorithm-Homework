//* Introduction Graph

/*
? คือ 
- กราฟ คือ โครงสร้างข้อมูลที่ใช้สำหรับเก็บ "ความสัมพันธ์" ระหว่างสิ่งต่างๆ โดยประกอบไปด้วย 2 ส่วนหลักๆ คือ:
    ● Vertex (หรือ Node): คือ "จุดยอด" หรือตัวแทนของสิ่งของ/ข้อมูล เช่น คน, เมือง, หรือสถานีรถไฟฟ้า
    ● Edge: คือ "เส้นเชื่อม" ที่ลากเชื่อมระหว่าง Vertex เพื่อบอกว่าสองสิ่งนั้นมีความสัมพันธ์กัน หรือมีเส้นทาง
      ไปหากันได้
@ ประเภทของกราฟที่พบบ่อย:
1. Directed Graph (กราฟแบบมีทิศทาง): เส้นเชื่อมจะมีหัวลูกศรบอกทิศทาง (ไปได้ทางเดียว คล้ายถนนวันเวย์) 
2. Undirected Graph (กราฟแบบไม่มีทิศทาง): เส้นเชื่อมไม่มีลูกศร คือไปและกลับได้ทั้งสองทาง (คล้ายถนนปกติ)
3. Weighted Graph (กราฟมีน้ำหนัก): เส้นเชื่อมจะมีตัวเลขกำกับ (น้ำหนัก) บอกค่าใช้จ่ายหรือระยะทาง เช่น 
   กราฟที่ใช้ในปัญหาเส้นทางสั้นที่สุด
4. Unweighted Graph (กราฟไม่มีน้ำหนัก): เส้นเชื่อมไม่มีตัวเลขกำกับ ถือว่าทุกเส้นเชื่อมมีค่าเท่ากัน
? การแทนกราฟในโปรแกรม:
@ 1. Adjacency Matrix (เมทริกซ์เชื่อมต่อ): 
- ใช้ตาราง 2 มิติ (อาร์เรย์ 2 มิติ) ขนาด VxV (V = จำนวน Vertex) 
    ● graph[i][j] = 1 / weight หมายถึงมีเส้นเชื่อมจาก Vertex i ไปยัง Vertex j (สำหรับ Directed 
      Graph) หรือมีเส้นเชื่อมระหว่าง i กับ j (สำหรับ Undirected Graph)
    ● graph[i][j] = 0 หมายถึงไม่มีเส้นเชื่อมระหว่าง i กับ j
* ตัวอย่างการสร้าง Adjacency Matrix:
- สมมติเรามีกราฟที่มี 5 Vertex (0, 1, 2, 3, 4) และมีเส้นเชื่อมดังนี้:
    ● 0 เชื่อมกับ 1
    ● 0 เชื่อมกับ 2
    ● 1 เชื่อมกับ 3
    ● 2 เชื่อมกับ 1
    ● 2 เชื่อมกับ 4
    ● 3 เชื่อมกับ 4
- Adjacency Matrix จะเป็น:
    0 1 1 0 0 → Vertex 0 เชื่อมกับ 1 และ 2
    0 0 0 1 0 
    1 1 0 0 1 → Vertex 2 เชื่อมกับ 0, 1 และ 4
    0 0 1 0 1
    0 1 0 0 0
* Pro & Con Adjacency Matrix:
- ข้อดี: 
    ● การตรวจสอบว่ามีเส้นเชื่อมระหว่างสอง Vertex หรือไม่ทำได้ใน O(1) เพราะแค่ดูค่า graph[i][j]
    ● เหมาะสำหรับกราฟที่มีจำนวนเส้นเชื่อมเยอะ (Dense Graph) เพราะใช้พื้นที่ O(V^2) อยู่แล้ว
- ข้อเสีย:
    ● เปลืองพื้นที่มากสำหรับกราฟที่มีจำนวนเส้นเชื่อมน้อย (Sparse Graph) เพราะต้องเก็บค่า 0 จำนวนมาก
    ● การวนลูปเพื่อหาทุก Vertex ที่เชื่อมกับ Vertex i ต้องใช้ O(V) เพราะต้องดูทั้งแถว graph[i][j]
@ 2. Adjacency List (รายการเชื่อมต่อ):
- ใช้โครงสร้างข้อมูลแบบลิสต์ (เช่น vector หรือ linked list) เพื่อเก็บรายชื่อของ Vertex ที่เชื่อมต่อกับแต่ละ 
  Vertex โดย graph[i] จะเป็นลิสต์ของ Vertex ที่เชื่อมต่อกับ Vertex i
* ตัวอย่างการสร้าง Adjacency List:
- สมมติเรามีกราฟที่มี 5 Vertex (0, 1, 2, 3, 4) และมีเส้นเชื่อมดังนี้:
    ● 0 เชื่อมกับ 0 (Loop คือ Vertex เชื่อมกับตัวเอง)
    ● 0 เชื่อมกับ 1
    ● 0 เชื่อมกับ 2
    ● 1 เชื่อมกับ 3
    ● 2 เชื่อมกับ 1
    ● 2 เชื่อมกับ 4
    ● 3 เชื่อมกับ 4
- Adjacency List จะเป็น:
    0 1 2   → graph[0] = {0, 1, 2} → Vertex 0 เชื่อมกับ 0, 1 และ 2
    1 3     → graph[1] = {3}    → Vertex 1 เชื่อมกับ 3
    2 1 4   → graph[2] = {1, 4} → Vertex 2 เชื่อมกับ 1 และ 4
    3 4     → graph[3] = {4}    → Vertex 3 เชื่อมกับ 4
    4       → graph[4] = {}     → Vertex 4 ไม่มีการเชื่อมต่อกับใคร
* Pro & Con Adjacency List:
- ข้อดี:
    ● ประหยัดพื้นที่มากสำหรับกราฟที่มีจำนวนเส้นเชื่อมน้อย (Sparse Graph) เพราะเก็บเฉพาะ Vertex ที่มีเส้นเชื่อม
    ● การวนลูปเพื่อหาทุก Vertex ที่เชื่อมกับ Vertex i ทำได้เร็วกว่า O(V) เพราะดูแค่ลิสต์ graph[i]
- ข้อเสีย:
    ● การตรวจสอบว่ามีเส้นเชื่อมระหว่างสอง Vertex หรือไม่ต้องใช้ O(k) (k = จำนวน Vertex ที่เชื่อมกับ i) 
      เพราะต้องดูในลิสต์ graph[i]
    ● เหมาะสำหรับกราฟที่มีจำนวนเส้นเชื่อมน้อย (Sparse Graph) เพราะใช้พื้นที่ O(V + E) (V = จำนวน 
      Vertex, E = จำนวน Edge)
*/