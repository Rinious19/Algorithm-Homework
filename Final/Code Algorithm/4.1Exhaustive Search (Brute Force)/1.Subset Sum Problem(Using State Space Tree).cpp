#include <bits/stdc++.h>
using namespace std;
 
int fnCallCount = 0; //* นับจำนวนครั้งที่ฟังก์ชันถูกเรียกใช้ (2^(n+1) - 1)
int subsetCount = 0; //* นับจำนวน subset ทั้งหมดที่ตรวจสอบ(Base Case) (2^n)

//? ฟังก์ชันนี้มีหน้าที่ "ตรวจคำตอบ" เมื่อเราเลือกตัวเลขครบทุกตัวแล้ว
void checkAndPrint(int binary[], int n, int arr[], int targetSum){
    int sum = 0;
    //@ 1. วนลูปดูสวิตช์ (binary) ทั้งหมด 5 ตัว (0 ถึง n-1)
    for(int i = 0; i < n; i++){
        if(binary[i] == 1) //* ถ้าสวิตช์ตัวที่ i เป็น 1 (แปลว่าเราเลือกตัวเลขนี้)
            sum += arr[i]; //* เอาค่าจาก arr[i] มาบวกเข้า sum
    }
    //@ 2. เช็คว่าผลรวมเท่ากับเป้าหมาย (12) หรือไม่?
    if(sum == targetSum){
        //* ส่วนนี้คือการจัดรูปแบบการพิมพ์ให้สวยงาม (เช่น "10 + 2 = 12")
        bool first = true;
        for(int i = 0; i < n; i++){
            if(binary[i] == 1){
                if(!first) cout<<" + "; //* พิมพ์เครื่องหมายบวก ถ้าไม่ใช่ตัวแรก
                cout<<arr[i];
                first = false;
            }
        }
        cout<<" = "<<targetSum<<endl;
    }
}

//? ฟังก์ชันนี้คือหัวใจหลัก (Recursive) ทำหน้าที่ "ตัดสินใจ" ทีละตัว
//* index: คือ "เข็มชี้" ว่าตอนนี้เรากำลังตัดสินใจที่ตัวเลขตัวไหน (0, 1, 2, 3, 4)
void generateSubsets(int binary[], int index, int n, int arr[], int targetSum){
    fnCallCount++;
    //@ Base Case: ถ้า index เท่ากับ n แปลว่าตัดสินใจครบ 5 ตัวแล้ว (เดินมาสุดทาง)
    if(index == n){
        subsetCount++;
        checkAndPrint(binary, n, arr, targetSum); //* ไปตรวจคำตอบซิว่าผลรวมได้เท่าไหร่
    }
    //@ จุดแตกกิ่ง (Recursive Step)
    else{
        //* ทางเลือกที่ 1: "เอา" ตัวเลขตำแหน่งนี้ (Set เป็น 1) 
        binary[index] = 1; //! ถ้าสลับเป็น 1 และ ข้างล่างเป็น 0 จะเริ่มจาก 0000... แทน 1111...
        generateSubsets(binary, index + 1, n, arr, targetSum); //* ขยับเข็ม (index+1) ไปทำตัวต่อไป
        //* ทางเลือกที่ 2: "ไม่เอา" ตัวเลขตำแหน่งนี้ (Set เป็น 0)
        //* นี่คือการ Backtrack (ย้อนกลับมาเปลี่ยนใจ)
        binary[index] = 0;
        generateSubsets(binary, index + 1, n, arr, targetSum); //* ขยับเข็ม (index+1) ไปทำตัวต่อไป
    }
}

int main(){
    int n = 5; //* จำนวนสมาชิกทั้งหมด
    int binary[n]; //* Array ที่เก็บสถานะ (0 หรือ 1) ใช้แทนการเลือก/ไม่เลือก
    int arr[5] = {25, 10, 9, 2, 1}; 
    int targetSum = 12; //* เป้าหมายผลรวม
    generateSubsets(binary, 0, n, arr, targetSum); //* เริ่มต้นเรียกฟังก์ชัน: เริ่มที่ index 0 (ตัวแรกสุด)
    cout<<"Function Call Count = "<<fnCallCount<<", Subset Count = "<<subsetCount<<endl;
    return 0;
}

/*
@ Vocabulary
1. Binary Decision: ในแต่ละสมาชิก (n) ตัวของอาเรย์ อัลกอริทึมต้องตัดสินใจ 2 ทางเลือก เสมอ (คือ เลือก 1
   หรือ ไม่เลือก 0)
2. State Space Tree: โครงสร้างการทำงานจะเป็น Tree ที่แตกออกเป็น 2 กิ่งในทุกระดับ ทำให้จำนวน Node 
   ทั้งหมดใน Tree เท่ากับ 2^0 + 2^1 + ... + 2^n = 2^(n + 1) - 1
3. Backtracking (การย้อนรอย) ใช้การ Recursive เพื่อลองเลือกสมาชิกทีละตัวลงไปใน State Space Tree 
   เมื่อทำงานในกิ่งนั้นจนสุด (หรือเรียกฟังก์ชันเสร็จ) มันจะทำการ "ย้อนกลับ" มาเพื่อเลือกตัวเลือกอื่นในลูป for ถัดไป
@ หลักการ
- สร้าง "Power Set" (เซตของทุกเซตย่อยที่เป็นไปได้)
- เช่น A = {1, 2, 3, 4}, P(A) = {∅, {1}, {2}, {3}, {4}, {1, 2}, {1, 3}, {1, 4}, {2, 3},
  {2, 4}, {3, 4}, {1, 2, 3}, {1, 2, 4}, {1, 3, 4}, {2, 3, 4}, {1, 2, 3, 4}}
* หลักการคือ "การลองเปิด-ปิดสวิตช์":
    - binary[] เปรียบเสมือนแผงสวิตช์ไฟ
        ● 1 คือ เปิดไฟ (เลือกตัวเลขนั้น)
        ● 0 คือ ปิดไฟ (ไม่เลือกตัวเลขนั้น)
@ ภาพรวมการทำงาน (Tracing)
1. เริ่มที่ index = 0 (เลข 25):
    ● โปรแกรมจะลองตั้ง binary[0] = 1 (เอา 25) แล้วกระโดดไป index = 1
    ● หลังจากทำกิ่งนั้นเสร็จ มันจะถอยกลับมาตั้ง binary[0] = 0 (ไม่เอา 25) แล้วกระโดดไป index = 1 อีกรอบ
2. การเดินของ index:
    ● มันจะเดินหน้าไปเรื่อยๆ 0 -> 1 -> 2 -> 3 -> 4 -> 5
    ● พอถึง 5 (เท่ากับ n) มันจะหยุดแล้วเรียก checkAndPrint
    ● จากนั้นมันจะถอยหลังกลับมาเปลี่ยนค่า 0/1 แล้วเดินหน้าใหม่ (วนลูปแบบนี้จนครบทุกรูปแบบที่เป็นไปได้ 
      2^5 = 32 แบบ)
@ ตัวอย่างการทำงาน
- มีข้อมูล 4 ตัว สถานะใน array binary จะเปลี่ยนไป ดังนี้
  1.  [1 1 1 1] -> เอาทุกตัว
  2.  [1 1 1 0]
  3.  [1 1 0 1]
  4.  [1 1 0 0]
  5.  [1 0 1 1]
  6.  [1 0 1 0]
  7.  [1 0 0 1]
  8.  [1 0 0 0]
  9.  [0 1 1 1]
  10. [0 1 1 0]
  11. [0 1 0 1]
  12. [0 1 0 0]
  13. [0 0 1 1]
  14. [0 0 1 0]
  15. [0 0 0 1]
  16. [0 0 0 0] -> ∅
@ การนับการทำงานของ Function
1. ถ้านำ count++ ไว้ที่บรรทัดแรกของ fn: 
    ● นับทุก Node: ไม่ว่าจะเป็นใบ (Base Case) หรือกิ่งก้าน (Recursive Step) จะถูกนับทั้งหมด
      เช่น สมมติเรามีเลข 2 ตัว คือ {1, 2}
          - ระดับ 0: เริ่มต้น (เรียกฟังก์ชันครั้งที่ 1) - index 0
          - ระดับ 1: ตัดสินใจตัวที่ 1 (เรียกเพิ่ม 2 ครั้ง) - index 1
          - ระดับ 2 (Base Case): ตัดสินใจตัวที่ 2 เสร็จแล้ว (เรียกเพิ่ม 4 ครั้ง) - index 2
          * รวม 1 + 2 + 4 = 2^(2 + 1) - 1 = 7 ครั้ง
    ● count = 2^(n + 1) - 1;
2. วางใน if(index == n): จะได้จำนวน Leaf Nodes (จำนวน Subset ทั้งหมดที่เป็นไปได้) ซึ่งเท่ากับ 2^n 
   (เช่น n=5 จะได้ 32) 
@ Time Complexity
- Big O = 2^n
*/