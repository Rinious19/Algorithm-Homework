//* Huffman Coding(Data compression)

/*
? คือ 
- มันคืออัลกอริทึมสำหรับ บีบอัดข้อมูล (Data Compression) แบบ Lossless (ข้อมูลไม่สูญหาย) 
- แทนที่จะใช้ 8-bit (ASCII) เท่ากันทุกตัวเหมือนปกติ เราจะเปลี่ยนมาใช้จำนวน Bit ที่แปรผันตามความถี่แทน 
  เพื่อให้ขนาดไฟล์รวมเล็กลง
- ใช้หลักการของต้นไม้ไบนารี (Binary Tree) ในการสร้างรหัสที่สั้นสำหรับข้อมูลที่มีความถี่สูง และรหัสที่ยาวสำหรับข้อมูลที่มีความถี่ต่ำ
- โดยใช้หลักว่า: 
    ● ตัวอักษรที่ “โผล่บ่อย” ควรใช้บิตสั้น
    ● ตัวอักษรที่ “โผล่น้อย” ใช้บิตยาว
? ขั้นตอนการทำ
1. นับความถี่ของแต่ละตัวอักษรในข้อมูลที่ต้องการบีบอัด
2. สร้างโหนดสำหรับแต่ละตัวอักษรและความถี่
3. นำโหนดทั้งหมดมาใส่ในโครงสร้างข้อมูลแบบ Min-Heap เพื่อให้สามารถดึงโหนดที่มีความถี่ต่ำสุดได้ง่าย
4. ทำการรวมโหนดที่มีความถี่ต่ำสุดสองโหนด
    - สร้างโหนดใหม่ที่มีความถี่เท่ากับผลรวมของสองโหนดนั้น
    - โหนดใหม่จะเป็นพ่อของสองโหนดที่ถูกนำมารวมกัน
    - นำโหนดใหม่กลับเข้าไปใน Min-Heap
5. ทำซ้ำขั้นตอนที่ 4 จนเหลือโหนดเดียว ซึ่งจะเป็นรากของต้นไม้ Huffman
6. กำหนดรหัสให้กับแต่ละตัวอักษรโดยการเดินทางจากรากไปยังโหนดที่แทนตัวอักษรนั้น ๆ
    - การเดินทางไปทางซ้ายจะถูกแทนด้วย '0' และการเดินทางไปทางขวาจะถูกแทนด้วย '1'
    - ตัวอักษรที่มีความถี่สูงจะอยู่ใกล้รากมากกว่า ทำให้ได้รหัสที่สั้นกว่า
@ ขั้นตออนการทำ
- สมมติเรามีคำว่า: "BANANA"
1. นับความถี่: B = 1, N = 2, A = 3
2. เรียงลำดับจากน้อยไปมาก: B (1), N (2), A (3)
3. สร้าง Tree (จากล่างขึ้นบน):
    ● หยิบ 2 ตัวที่น้อยที่สุดมาสร้าง Node ใหม่: B(1) + N(2) = 3
    ● ตอนนี้เราจะมี Node ใหม่ (3) และ A (3)
    ● รวม Node (3) + A (3) = Root (6)
4. กำหนดรหัส (0 และ 1):
    ● ให้กิ่งซ้ายเป็น 0 และกิ่งขวาเป็น 1 (หรือสลับกันก็ได้แต่ต้องเหมือนกันทั้งต้น)
5. อ่านรหัส: ไล่จากบนสุดลงมาหาตัวอักษรนั้น ๆ
? ตัวอย่าง Huffman Coding
 ┌──────────────────────────────────────────────────────────────────┐
 │ ตัวอักษร   ความถี่    รหัสปกติ (3-bit)   รหัส Huffman                  │
 ├──────────────────────────────────────────────────────────────────┤
 │   A        45          000              0 (สั้นมากเพราะมาบ่อย)      │ 
 │   B        13          001            101                        │
 │   C        12          010            100                        │
 │   D        16          011            111                        │
 │   E         9          100            1101                       │
 │   F         5          101            1100 (ยาวหน่อยเพราะมาน้อย)   │
 └──────────────────────────────────────────────────────────────────┘
*/