//* Minimum Spanning Tree (MST)
 
/*
? คือ 
- ลองจินตนาการว่าคุณเป็นผู้ว่าราชการจังหวัด แล้วต้อง "เดินสายแลนเชื่อมอินเทอร์เน็ต" หรือ "สร้างถนน" ให้ครบ
  ทุกหมู่บ้าน โดยมีข้อแม้ว่าต้องใช้ความยาวสายแลนรวมกัน หรือใช้งบสร้างถนนรวมกันให้ น้อยที่สุด
* นิยาม
1. ต้นไม้แผ่ทั่ว (Spanning tree): หมายถึง กราฟที่ไม่มีวงวน (acyclic graph) ซึ่งเชื่อมโยงทุกเวอร์เท็กซ์ 
   (โหนด) เข้าด้วยกัน โดยใช้เส้นเชื่อมจำนวน |V| - 1 เส้น (จำนวนโหนดลบ 1)
2. ต้นไม้แผ่ทั่วเล็กที่สุด (Minimum spanning tree): คือต้นไม้แผ่ทั่วที่มีผลรวมของน้ำหนักเส้นเชื่อม (เอดจ์) 
   น้อยที่สุด
? Algorithm ที่นิยมใช้ในการหา MST:
@ 1. Prim's Algorithm (โตจากจุดศูนย์กลาง)
- คือ การเริ่มต้นจากโหนดใดโหนดหนึ่ง (สามารถเลือกได้) แล้วค่อยๆ ขยายเส้นทางไปยังโหนดที่อยู่ใกล้ที่สุด 
  ที่ยังไม่เคยไป โดยใช้ข้อมูลของเส้นเชื่อมที่มีน้ำหนักน้อยที่สุดในขณะนั้น
- ต่างจาก Dijkstra's Algorithm ที่เน้นการหาเส้นทางที่สั้นที่สุดจากจุดเริ่มต้นไปยังโหนดอื่นๆ (ระยะทางสะสม)
  Prim's Algorithm เน้นการสร้างต้นไม้แผ่ทั่วที่มีน้ำหนักรวมต่ำที่สุด โดยไม่สนใจเส้นทางที่สั้นที่สุดระหว่างโหนด
- คล้ายกับการโยนหินลงน้ำแล้วให้วงคลื่นค่อยๆ แผ่ขยายออกไป จากจุดที่โยนหินลงไป
* ตัวอย่างการทำงาน:
- สมมติมีหมู่บ้าน A, B, C, D
1. เริ่มต้น: สุ่มเลือกยืนที่หมู่บ้าน A
2. แผ่ขยายรอบ 1: มองไปรอบๆ A พบว่ามีทางไป B (ราคา 10), ไป C (ราคา 5)
    ● เราเลือก A → C (ราคา 5) เพราะถูกสุด ตอนนี้ต้นไม้เรามี A กับ C แล้ว
3. แผ่ขยายรอบ 2: มองออกจากต้นไม้ของเรา (A และ C) ไปหาจุดที่ยังไม่เชื่อม (B และ D)
    ● ทางออกมี: A → B (10), C → B (2), C → D (8)
    ● เราเลือก C → B (ราคา 2) ตอนนี้ต้นไม้เรามี A, C, B
4. แผ่ขยายรอบ 3: มองหาทางเชื่อมไป D (จุดเดียวที่เหลืออยู่)
    ● สมมติมีทาง B → D (ราคา 4), C → D (8)
    ● เราเลือก B → D (ราคา 4)
5. จบการทำงาน: ทุกหมู่บ้านเชื่อมกันหมดแล้ว ได้ MST ที่มีราคารวม 5 + 2 + 4 = 11
@ 2. Kruskal's Algorithm (ประกอบร่างจากเส้นที่ถูกที่สุด)
- หลักการ: ไม่สนใจจุดเริ่มต้น แต่จะกางแผนที่ดู "เส้นทาง (Edge)" ทั้งหมด แล้วเรียงลำดับเส้นทางจากราคา
  ถูกสุดไปแพงสุด จากนั้นหยิบเส้นทางที่ถูกที่สุดมาสร้างทีละเส้น โดยมีกฎเหล็กเพียงข้อเดียวคือ "ห้ามสร้างเส้นที่
  ทำให้เกิดวงวน (Cycle)" เด็ดขาด (เพราะถ้าเกิดวงวน แปลว่าเรากำลังเชื่อมโหนดที่เคยเชื่อมกันแล้ว ซึ่งไม่จำ
  เป็นและทำให้ต้นไม้ไม่เป็นต้นไม้)
- Cycle คือ สถานการณ์ที่เรากำลังเชื่อมโหนดที่เคยเชื่อมกันแล้ว เช่น A → B → C → A ซึ่งจะทำให้ต้นไม้ของ
  เราไม่เป็นต้นไม้
* ตัวอย่างการทำงาน:
- ใช้หมู่บ้านเดิม A, B, C, D แต่เราเอาเส้นทางทั้งหมดมากองรวมกันแล้วเรียงลำดับราคา:
    ● เส้น C → B (ราคา 2)
    ● เส้น B → D (ราคา 4)
    ● เส้น A → C (ราคา 5)
    ● เส้น C → D (ราคา 8)
    ● เส้น A → B (ราคา 10)
1. หยิบเส้นที่ 1: C → B (ราคา 2) → วาดลงแผนที่ (ไม่เกิดวงวน)
2. หยิบเส้นที่ 2: B → D (ราคา 4) → วาดลงแผนที่ (ไม่เกิดวงวน)
3. หยิบเส้นที่ 3: A → C (ราคา 5) → วาดลงแผนที่ (ไม่เกิดวงวน)
4. หยิบเส้นที่ 4: C → D (ราคา 8) → ทิ้ง! เพราะ C กับ D มันสามารถเดินทางถึงกันได้ผ่าน B อยู่แล้ว ถ้าขีดเส้น
   นี้มันจะกลายเป็นวงวน (Cycle) ทันที
5. จบการทำงาน: เราได้เส้นทางครบ |V| - 1 เส้นแล้ว (4 หมู่บ้าน ต้องการ 3 เส้น) ผลรวมคือ 2 + 4 + 5 = 11
   เท่ากับวิธีของ Prim เป๊ะๆ
*/